/**
 * Use pnpm create vite@latest and then a dot at the end to create the project in the current folder itself, that dot will not create one more folder. 
 * 
 * The order of plugins in vite.config.ts files defines the execution of the those plugins. 
 * 
 * Use proper html tags, section tag, nav tag, main tag (learn html tags and why which tag is used?)
 * 
 * We have to give meta data according to each page
 * 
 * 
 * Use tricks like, if someone tried to access a protected route without login, redirect them to login page and after login redirect them to the protected route they wanted to access.
 * 
 * 
 * Have well knowledge of what all libraries are out there and where to use them?
 * 
 * 
 */